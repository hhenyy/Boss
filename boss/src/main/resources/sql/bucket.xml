<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="boss.dao.BucketDao">

<!-- 	<insert id="insert" parameterType="boss.model.Bucket">
		INSERT INTO bucket (
		BID, MEMAIL, PID, BNAME, BIMAGE, BCOUNT, BSIZE, BCOLOR, BPRICE, BDROP
		) VALUES (
		bucket_seq.NEXTVAL, #{mEmail}, #{pId}, #{bName}, #{bImage}, #{bCount}, #{bSize},
		#{bColor}, #{bPrice}, 'N'
		)
	</insert>

	<update id="update" parameterType="boss.model.Bucket">
		UPDATE bucket
		SET BCOUNT = BCOUNT + #{bCount}
		WHERE MEMAIL = #{mEmail} AND PID = #{pId} AND BDROP = 'N'
	</update> -->

</mapper>
